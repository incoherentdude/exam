add harddrive
see whether it is show
sudo fdisk -l
sudo mkfs.ext4 <dev/sdb -name>
mkdir /mnt/exam
sudo mount <dev/sdb1> /mnt/exam
add two images to /mnt/exam ( in .jpg format)
delete 1 image
sudo dd if=dev/sdba of=exam.img
xxd -a exam.img | grep 'ffd8"
xxd -a exam.img | grep 'ffd9"

go to gui make partition 
/mount 
































































































































































































































































































































































































































































add image
